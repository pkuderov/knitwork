# HarmonicGridRNN

## Описание

**HarmonicGridRNN** — гармоническая Grid RNN, объединяющая четыре смысловых блока в единую архитектуру. Название отражает идею «гармоник»: каждый блок работает на своей «частоте» и усиливает остальные — как обертоны акустического сигнала складываются в богатый звук. Цель модели — преодолеть ограничения существующих архитектур: `hgrnn_lru` отлично работает на SDQ, но нестабильна в RL; `grnn_ema_mem` выигрывает в MIKASA, но слабее в ассоциативных задачах; `grnn_delta` теоретически верна, но не обучается из-за сложности оптимизации. Гармоническая модель объединяет лучшее из каждой, не наследуя их слабостей.

## Четыре блока

| # | Блок | Обучаем? | Роль |
|---|------|---------|------|
| 1 | Spectral LRU | ✓ | 2D иерархия временны́х масштабов |
| 2 | Surprise-Delta Memory | ✓ | Адаптивная KV-память без интерференции |
| 3 | Frozen Reservoir | ✗ | Долгосрочный контекст (текст) |
| 4 | Hopfield Integration | ✓ | Резкая кросс-колоночная ассоциативная выборка |

## Ключевые механизмы

### Block 1: Spectral LRU

Каждая колонка и каждый слой имеют собственный `r_max`, формируя 2D спектральную сетку. Нижние слои и левые колонки — быстрые (низкий r), верхние и правые — медленные (высокий r). Это обеспечивает представления на разных временны́х масштабах без дополнительных параметров.

```python
# r_max[layer, col] = r_min_col + (r_base_layer - r_min_col) * (col / (C-1))
# r_base_layer интерполирован от r_min_layers до r_max_layers
```

### Block 2: SurpriseDeltaMemory — ключевой новый модуль

Объединяет принципы из `grnn_ema_mem` (EMA-гейт записи) и `grnn_delta/prec_delta` (дельта-правило). Пишет в общую матрицу `W [B, dk, dv]` пропорционально ошибке предсказания, используя дельта-правило для устранения интерференции.

```python
# 1. Параллельное дельта-правило на замороженном W (нет chained Jacobians):
v = F.normalize(proj_v(y), dim=-1)  # нормировано → ||delta_W||_F ограничен
v_pred = W.T @ k[c]
error  = v[c] - v_pred              # ошибка предсказания ∈ [-2, 2]
delta_W += k[c] ⊗ error             # outer product

# 2. EMA surprise — отслеживает "насколько неожиданным" был ввод:
m_new = ema_beta * m + (1-ema_beta) * mean(error²)
alpha = (m_new / max(m_new + eps)).clamp(0, 1)  # нормировка по батчу

# 3. Adaptive forgetting (из grnn_ema_mem):
fullness = ||W||_F / sqrt(dk * dv)
lam = lam_base * fullness.clamp(0, 1)

# 4. Delta update (per-layer delta_decay: 0.95 → 0.99):
W_new = (1-lam) * delta_decay[layer] * W + alpha * delta_W / C
```

**Числовая стабилизация (v2)**:
- `delta_W /= C` — критически важно: без нормировки effective eigenvalue = `decay - alpha*C = 0.99 - 4 = -3.01` → W взрывается. С `/C`: eigenvalue = `decay - alpha ≈ -0.01` → сходится.
- v нормируется (F.normalize) → delta_W ограничен
- alpha = m / max(m) вместо sigmoid(log(m)) — нет заморозки записей при m≈0
- `out_norms[l]` — inter-layer LayerNorm предотвращает рост h между слоями
- per-layer delta_decay: нижние слои забывают быстрее (0.95), верхние — медленнее (0.99)

### Block 3: Frozen Reservoir

Фиксированные случайные RNN-колонки с разными спектральными радиусами. При `r=0.999` время памяти ≈ 1000 шагов. Для SDQ/RL отключается (`n_reservoir_cols=0`).

### Block 4: Hopfield Cross-Column Integration

Modern Hopfield Networks с learnable β per head. Принимает все обучаемые колонки + проекции резервуара.

### Inter-layer normalization

После каждого слоя `y_out` нормируется (LayerNorm) перед подачей в следующий слой — предотвращает экспоненциальный рост магнитуды.

## Гиперпараметры

- `r_min_col` (0.3), `r_min_layers` (0.7), `r_max_layers` (0.999) — границы 2D спектральной иерархии
- `dk` / `dv` — размерности ключей/значений памяти (по умолчанию H//4 и H)
- `ema_beta` (0.9) — EMA для записи памяти; v3: per-layer от `ema_beta_min` до `ema_beta`
- `ema_beta_min` (0.7, v3) — beta для нижних слоёв; верхние получают `ema_beta`
- `delta_decay` (0.99) / `delta_decay_min` (0.95) — max/min decay W; интерполируются по слоям
- `lam_base` (0.01) — базовая скорость забывания
- `n_reservoir_cols` (0 для SDQ/RL, 4 для текста)

## Состояние

```python
HarmonicState(
    h:     [L, C, B, 2H]        # LRU-состояния (Re | Im); Im часть детачится
    h_res: [L, C_res, B, H_res] # резервуар; всегда детачится
    W:     [L, B, dk, dv]       # матрица дельта-памяти
    m:     [L, B]               # EMA surprise per layer
)
```

## Результаты экспериментов

### v2 — SDQ (grnn_harmonic, 2.11M params, 3L×4C, нет резервуара)

Эксперимент #043, запущен 2026-06-10, 300M шагов из 1000M.

| Шаг | Loss | Acc | Acc/query | Acc/distract |
|-----|------|-----|-----------|--------------|
| 5M  | 1.281 | 0.519 | 0.241 | — |
| 50M | 0.783 | 0.684 | 0.534 | 0.901 |
| 100M | 0.636 | 0.746 | 0.622 | 0.964 |
| 200M | 0.573 | 0.779 | 0.659 | 0.983 |
| 300M | 0.563 | 0.780 | 0.675 | 0.983 |

**Наблюдения**: Acc/distract=0.983 (почти идеально), но Acc/query=0.675 — плато с ~200M шагов.
Бутылочное горлышко — ретривал, а не отвлечение. `As` (Acc/store) = NaN — маска
`sq_gaps < -1.0` не срабатывает (вторичная проблема логирования).

### v2 — Text Shakespeare (grnn_harmonic, 2.22M params, 4 резервуарных колонки)

Эксперимент #044, запущен 2026-06-10, 340M шагов из 520M.

| Шаг | Loss | BPC | Acc |
|-----|------|-----|-----|
| 50M | 0.930 | 1.342 | 0.749 |
| 100M | 0.827 | 1.193 | 0.773 |
| 200M | 0.692 | 0.998 | 0.799 |
| 300M | 0.594 | 0.857 | 0.807 |
| 340M | 0.547 | 0.788 | 0.823 |

**Наблюдения**: стабильный линейный спуск, без плато. BPC=0.788 — хороший результат.
T (контекст сброса) растёт до ~910 шагов, что соответствует длинным сессиям.

### v2 — Text text8 (2.21M params, 4 резервуарных колонки)

Эксперимент #045, запущен 2026-06-11, 215M шагов из 520M.

| Шаг | Loss | BPC | Acc |
|-----|------|-----|-----|
| 50M | 1.255 | 1.811 | 0.604 |
| 100M | 1.225 | 1.767 | 0.611 |
| 150M | 1.204 | 1.737 | 0.620 |
| 215M | 1.169 | 1.687 | 0.631 |

**Наблюдения**: очень медленный прогресс — 0.080 BPC за 165M шагов. Сравнение с Shakespeare
(0.554 BPC за 340M) показывает фундаментальное отставание. `rollout_len=8` даёт ~1.5 слова
TBPTT-контекста — критически мало для text8.

## Проблемы

### 1. Только колонка 0 идёт в голову

```python
y_top = y_out[0]   # только самая быстрая колонка (r≈0.7)
```

Колонки 1-3 (r≈0.999, τ≈1000 шагов) накапливают долгосрочный контекст, но их выход
никогда не попадает в предсказание. Это прямая причина плато Acc/query в SDQ: запросы
требуют долгосрочного ретривала из медленных колонок, которые игнорируются.

### 2. Колонки 1-3 получают нулевой вход на слое 0

```python
return [x] + [dummy] * (self.n_columns - 1)   # dummy = zeros
```

На первом слое колонки 1-3 обрабатывают только нули, получая информацию лишь через
Hopfield-messaging от колонки 0. Это информационное бутылочное горлышко.

### 3. EMA beta не согласован с LRU-масштабом

Все слои используют `ema_beta=0.9` (τ≈10 шагов). Но верхний слой имеет LRU r≈0.999
(τ≈1000 шагов) — медленный рекуррентный блок с быстро перезаписываемой памятью.
Новые события вытесняют долгосрочные ассоциации раньше, чем LRU их «оценивает».

### 4. text8: rollout_len=8 слишком короткий

8 символов = ~1.5 слова BPTT-контекста. text8 — непрерывный поток без пунктуации,
средняя длина слова 5 символов. Нужно 16-32 символа для захвата слово-уровневых паттернов